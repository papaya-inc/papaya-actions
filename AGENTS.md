# Reusable Actions repository instructions

Before work, read `../papaya-docs/README.md`,
`../papaya-docs/repositories/papaya-actions.md`, and `README.md`.

Keep workflows generic and validate changes through an explicit consuming
repository. Application-specific behavior stays with that application. Update
`papaya-docs` when the shared workflow contract changes.
